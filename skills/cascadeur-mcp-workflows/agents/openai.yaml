interface:
  display_name: "Cascadeur MCP Workflows"
  short_description: "Plan and execute complete Cascadeur animation workflows"
  default_prompt: "Use $cascadeur-mcp-workflows to plan, execute, verify, and safely recover this Cascadeur task."

dependencies:
  tools:
    - type: "mcp"
      value: "cascadeur-complete"
      description: "Local Cascadeur 2026.1.x control and verification server"

policy:
  allow_implicit_invocation: true
